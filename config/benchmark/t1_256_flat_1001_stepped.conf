# T1 example benchmark scenario, played in stepped mode.
#
# T1: 366 steps of one game day, 4 completed quarters, .
# the build-skill tier, largely pre-revenue: two quarters mostly measures whether a
# working route stands at all.
#
# The world is 256x256 flat seed 1001, shared with t1_256_flat_1001_realtime, so the two differ only
# in who owns the clock. See README.md in this directory for the tier table, why the day counts
# are what they are, and why a stepped run is not bounded by wall time.
#
#   uv run nttd benchmark --config config/benchmark/t1_256_flat_1001_stepped.conf
#
# Your loop then drives it:
#   POST /v1/participant/sessions/{id}/step/reset   -> opening observation
#   POST /v1/participant/sessions/{id}/step         -> flush actions, advance one day, observe

scenario {
  name        = "benchmark-t1-256-flat-1001-stepped"
  description = "T1 benchmark, stepped: 366 steps of 1 game-day, 4 completed quarters"
  id          = "benchmark-t1-256-flat-1001-stepped"

  map {
    size_x       = 256
    size_y       = 256
    terrain_type = "flat"
    seed         = 1001
  }

  companies {
    # No AI opponents, by design: nttd measures building a business in an empty market.
    num_ai_companies     = 0
    competitors_interval = 0
    max_loan             = 300000
  }

  runtime {
    mode                   = "stepped"
    snapshot_interval_days = 1
  }

  heartbeat {
    # One game day per step. A step flushes its actions while the game is paused, so a batch
    # of any size still advances exactly one day.
    interval_days = 1
  }

  end_conditions {
    logic = "any"
    # Bounded in steps, not wall time: the clock only moves when the contestant asks.
    time_limit {
      enabled = false
    }
    max_heartbeats {
      enabled = true
      count   = 366
    }
    bankruptcy {
      enabled = true
    }
  }
}
