# nttd autoexec — runs when OpenTTD starts with this config directory.
# OpenTTD console scripts use '#' for comments, not ';'.
# Add console commands below (e.g. 'setting ...') if needed at startup.
